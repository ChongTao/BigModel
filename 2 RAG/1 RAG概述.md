# 一 RAG 是什么

RAG（Retrieval-Augmented Generation，检索增强生成）是一种把 **外部知识检索** 和 **大模型生成** 组合起来的方案。它的目标不是让模型只依赖参数记忆回答问题，而是在回答前先从知识库、文档库、API 或数据库中找到相关信息，再把这些信息作为上下文交给模型生成答案。

RAG 通常包含四个核心环节：

1. 查询理解：理解用户问题、任务目标和约束条件。
2. 检索：从外部知识源中找出与问题最相关的候选内容。
3. 增强：将候选内容筛选、压缩、组织后拼接到上下文中。
4. 生成：大模型结合检索结果生成最终回答。

> 除了以上四步，RAG 还依赖一个基础能力：Embedding 编码。它负责把文本转成向量，供向量检索、相似度计算和语义匹配使用。

RAG 的处理流程如下图：

![](https://www.dailydoseofds.com/content/images/size/w1000/2024/10/rag.gif)

典型价值：

- 实时性：可以接入最新文档和外部数据，不受训练截止时间限制。
- 准确性：让回答建立在可检索证据上，降低纯生成带来的幻觉。
- 可解释性：可以返回引用片段、来源文档或检索证据。
- 轻量化：相比重新训练或微调全量模型，更新知识库的成本通常更低。

RAG 的核心指标通常分成两类：

- 检索侧：召回率、Precision@K、MRR、nDCG。
- 生成侧：答案准确性、相关性、忠实度（faithfulness）、引用有效性。

> 在大语言模型应用中，RAG 主要用于缓解知识滞后、事实性不足和专业领域知识覆盖不够的问题，但它并不能自动解决所有推理问题，系统质量仍然取决于数据、检索和提示设计。

# 二 RAG 基本流程

一个最常见的 RAG 链路如下：

1. 文档预处理：清洗文本、提取元数据、切分 chunk。
2. 建索引：为 chunk 建立倒排索引、向量索引或混合索引。
3. 查询预处理：对 query 做改写、扩展、过滤或路由。
4. 初召回：使用关键词检索、向量检索或混合检索取回 Top-K。
5. 重排与压缩：对候选结果做 rerank、去重和上下文压缩。
6. 构造 Prompt：把问题、约束、检索结果和格式要求拼进提示词。
7. 生成回答：调用模型输出结果，并可附带引用或证据。

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1000/2024/11/image-2.png)

## 2.1 文档切分 Chunking

将知识文档切分chunk，然后将每个chunk进行向量化存储在向量库中，文档切分的目标有两个：

1. 控制上下文窗口和索引粒度。
2. 在“检索精度”和“上下文完整性”之间做平衡。

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1600/2024/11/image-6.png)

文档分块策略影响检索的质量，以下是常见的分块策略，更详细信息见[RAG分块策略](./4 RAG分块策略.md)

![](https://www.dailydoseofds.com/content/images/2024/11/chunking-rag.gif)

## 2.2生成向量 

分块之后，利用向量化模型对分块进行向量化。

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1000/2024/11/image-9.png)

## 2.3 存储向量化数据库

将数据存储在向量化数据库中：

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1000/2024/11/image-10.png)

## 2.4 用户输入查询

用户输入查询语句，使用上述数据块相同的向量化模型，将查询转换成向量。

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1000/2024/11/image-12.png)

## 2.5 检索相似块

将向量化查询与数据库中现有的向量进行比较，找到最相似的信息，向量库返回topK最相似的文档。

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1000/2024/11/image-13.png)



## 2.6 重排

检索后，可能需要对数据进行重拍，确保最相关的信息能够让模型看到，通常使用交叉编码器将检索之后的数据和query一起评估，从而为每个数据块计算一个相关分数。

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1600/2024/11/image-15.png)

## 2.7 生成

对最相关的数据进行重排后，将它们输入到LLM中，该模型会将用户原始查询与检索的信息结合给提示模板总，生成一个综合所选文档信息的响应。

![](https://storage.ghost.io/c/3f/df/3fdf6ed2-17ac-4b12-a693-8078bd13e748/content/images/size/w1600/2024/11/image-16.png)



# 三 常见问题与设计权衡

## 3.1 为什么需要 RAG

如果只依赖模型参数，通常会面临以下问题：

- **知识滞后**：模型无法天然知道训练后出现的新信息。
- **幻觉风险**：模型可能生成看似合理但缺乏证据的答案。
- **专业领域局限**：垂直领域知识覆盖不足，细节不可靠。
- **企业私有知识不可见**：内部文档、流程和数据不在公开训练语料中。

RAG 的价值是把“模型能力”与“外部知识”解耦，让知识可以单独更新和维护。

## 3.2 检索结果是否真的有用

检索命中并不等于问题可回答。一个结果可能在语义上接近 query，但缺少真正回答问题所需的事实、约束或上下文。

因此评估 RAG 时不能只看“检索到了没有”，还要看：

- 召回结果是否包含支持答案的证据。
- 排名前几条是否已经足够回答问题。
- 最终答案是否忠实于检索证据。

## 3.3 分块多大才合适

块太小，容易丢失关键上下文；块太大，又会引入大量无关信息，稀释生成效果。经验上可以这样理解：

- 事实型问答：倾向更小、更聚焦的 chunk。
- 解释型问答：需要更完整的上下文，可适当增大 chunk。
- 长文档检索：常结合分层分块或 Parent Document 策略。

## 3.4 RAG 不是万能解法

RAG 更擅长“找信息并基于证据回答”，但它并不天然擅长：

- 复杂规划和长链路决策。
- 没有外部证据时的纯推理问题。
- 依赖强操作反馈的任务执行。

# 四 参考

- https://www.dailydoseofds.com/a-crash-course-on-building-rag-systems-part-1-with-implementations/
