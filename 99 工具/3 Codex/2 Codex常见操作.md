# 2 Codex常见操作

## 2.1 启动 Codex

```sh
codex
```

进入交互式会话后，Codex 会在当前目录读取项目上下文、规则文件和配置。

## 2.2 在当前项目里协作

常见提示方式：

- 让它先理解目录结构
- 明确目标文件或模块
- 指定是否可以改文件、跑命令、写测试

例如：

```text
先阅读这个仓库的结构，说明入口、核心模块和数据流。
```

```text
修复当前分支里这个 bug，只修改最小范围代码，并补充必要测试。
```

## 2.3 查看当前规则是否生效

如果仓库里有 `AGENTS.md`，可以让 Codex 先总结当前加载到的规则：

```text
先总结当前会话已经加载的项目规则，再开始改动。
```

## 2.4 常见工作流

### 阅读代码

```text
先不要改代码，说明这个模块的职责、关键入口和依赖关系。
```

### 实现功能

```text
实现这个需求，先给出最小改动方案，再直接修改代码并验证。
```

### 做代码审查

```text
审查当前分支相对 main 的变化，重点看 bug、回归风险和测试缺口。
```

### 整理文档

```text
按当前目录风格拆分文档，并更新 README 入口。
```

## 2.5 交互原则

想让 Codex 稳定一些，通常有几个实用原则：

- 先说目标，再说限制
- 明确它能不能改文件、跑命令、联网
- 大任务先拆块
- 让它在动手前先总结现状
- 改完后要求它做体检

## 2.6 什么时候该用 Subagents

如果任务同时包含多条可并行子线，例如：

- 多个文件独立分析
- 多个 review 维度并行检查
- 多个模块独立调研

这时更适合显式让 Codex 使用子代理，而不是单线程硬做。
